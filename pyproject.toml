[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "seedhibaat"
version = "0.2.0"
description = "Self-hosted WhatsApp marketing automation for Shopify"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
seedhibaat = "seedhibaat.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
